# 变更记录

本文件记录项目的重要功能变化。

## 未发布

### 复分解析（第一阶段，非全书完成）

- 保留并固定原有分类依赖0.0.1，增加一次读取的只读适配层；主分类数据不被替代。
- 增加离线补充规则、第五版来源页码、候选索引和独立核对记录。
- 支持首批文学、传记、农作物仿分，以及已录入通用表的标记解析；修复I524.45和K835.635.72等路径。
- 规则版本更新至 `5.0.0-stage1.1`：修复 `I12(198.4)` 遗漏“古代希腊”；同类古代地区号解释与编目适用性提示分离，不再依赖类名中是否包含“史”。
- 为古代地区补充原号保留、未知/不完整尾缀、停用状态优先及单条/批量/Excel一致性回归测试。
- 将部分解析、停用号、交替号和历史地区分开标记，未知尾缀不再静默丢弃。
- 单条、批量和Excel统一展示状态、复分说明和未解析部分；书目成功不因路径不完整而重试。
- Excel“完整分类路径”改为“分类路径”，补充解析字段，以等号开头的内容按文字保存。
- 新增原库全部父链回归、规则正反例、API/导出测试和本地隔离页面验收夹具。
- 全书候选的逐条审核、其他专用表和复杂组配尚未完成，详见规则覆盖报告。

### 文档

- 重写 README，使部署、缓存、代理、测试和已知限制与当前代码一致。
- 将初始实现计划更新为当前实现说明。
- 明确旧式二进制 XLS 文件需要先转换为 XLSX。
- 发布候选索引仅保留页码、编号和审核元数据；OCR定位片段、源文件指纹及本机数据留在本地，不随仓库发布。

## 2026-08-25

### 新增

- 增加 SQLite 查询缓存：成功结果默认缓存 90 天，未收录结果缓存 24 小时。
- 增加国图动态 URL 的站点验证和多种页面地址格式兼容。
- 增加 9 项自动测试，覆盖缓存、会话复用、动态会话刷新、批量去重和熔断。

### 优化

- 单次批量任务复用 HTTP 会话、连接池和国图动态会话 URL。
- 为连接错误、读取错误、HTTP 429 和部分 5xx 状态增加自动重试与退避。
- 动态会话失效时自动刷新并额外查询一次。
- 同批重复 ISBN 只请求一次，同时保留全部输入记录。
- 缓存命中、重复 ISBN 和输入校验失败不再触发批量等待。
- 连续 3 次真实上游请求失败后暂停本批次后续网络查询。
- 修复 Excel 第一行被误当表头时少识别一条记录的问题。

### 已知限制

- 本地分类路径解析尚不能完整处理复分、仿分和组合分类号；原始中图分类号不受影响。
